[build]
builder = "nixpacks"
buildCommand = "cd backend && npm ci --omit=dev"

[deploy]
startCommand = "mkdir -p /app/backend/frontend && cp -r /app/frontend/build /app/backend/frontend/build && cd /app/backend && node src/index.js"
restartPolicyType = "on_failure"
